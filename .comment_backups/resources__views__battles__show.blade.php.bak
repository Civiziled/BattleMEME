<x-app-layout>
    <x-slot name="header">
        <div class="flex items-center justify-between">
            <div class="flex items-center">
                <a href="{{ route('battles.index') }}"
                   class="mr-4 text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white">
                    ← Retour aux battles
                </a>
                <h2 class="font-semibold text-xl text-gray-800 dark:text-gray-200 leading-tight">
                    🏆 {{ $battle->title }}
                </h2>
            </div>

            @auth
                @can('update', $battle)
                    <div class="flex gap-2">
                        <a href="{{ route('battles.edit', $battle) }}"
                           class="bg-yellow-600 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded-lg transition duration-200">
                            ✏️ Modifier
                        </a>
                        <form method="POST" action="{{ route('battles.destroy', $battle) }}"
                              onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette battle ? Cette action est irréversible.')">
                            @csrf
                            @method('DELETE')
                            <button type="submit"
                                    class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-lg transition duration-200">
                                🗑️ Supprimer
                            </button>
                        </form>
                    </div>
                @endcan
            @endauth
        </div>
    </x-slot>

    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8 mt-8">

            <!-- Informations de la battle -->
            <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg mb-8 pt-6">
                <div class="p-6 pt-0">
                    <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between mb-4">
                        <div class="flex-1 mb-4 lg:mb-0">
                            <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-2">
                                {{ $battle->title }}
                            </h1>
                            <p class="text-gray-600 dark:text-gray-300 mb-4">
                                {{ $battle->description }}
                            </p>
                        </div>
                        <div class="flex flex-col lg:items-end space-y-2">
                            <span class="px-3 py-1 text-sm font-semibold rounded-full
                                {{ $battle->isOpen() ? 'bg-emerald-100 text-emerald-800 dark:bg-emerald-900 dark:text-emerald-200' : 'bg-rose-100 text-rose-800 dark:bg-rose-900 dark:text-rose-200' }}">
                                {{ $battle->isOpen() ? '🟢 Battle Ouverte' : '🔴 Battle Fermée' }}
                            </span>
                            <div class="text-sm text-gray-500 dark:text-gray-400">
                                <div>👤 Créée par <strong>{{ $battle->user->name }}</strong></div>
                                <div>📅 Deadline: {{ $battle->deadline->format('d/m/Y à H:i') }}</div>
                                <div>🎭 {{ $battle->memes->count() }} mème(s) soumis</div>
                            </div>
                        </div>
                    </div>

                    @auth
                            @if($battle->isOpen())
                            <div class="flex flex-col sm:flex-row gap-4">
                                <a href="{{ route('memes.create', $battle) }}"
                                   class="w-full sm:w-auto bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200 shadow-lg">
                                    🎭 Soumettre un Mème
                                </a>
                            </div>
                        @else
                            <div class="bg-slate-50 dark:bg-slate-900/20 border border-slate-200 dark:border-slate-800 rounded-lg p-4">
                                <p class="text-slate-800 dark:text-slate-200">
                                    ⏰ Cette battle est fermée depuis le {{ $battle->deadline->format('d/m/Y à H:i') }}.
                                    Aucune nouvelle soumission n'est acceptée.
                                </p>
                            </div>
                        @endif
                    @else
                        <div class="bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg p-4">
                            <p class="text-blue-800 dark:text-blue-200">
                                🔐 <a href="{{ route('login') }}" class="underline hover:no-underline">Connectez-vous</a>
                                pour participer à cette battle !
                            </p>
                        </div>
                    @endauth
                </div>
            </div>

            <!-- Classement des mèmes -->
            @if($rankedMemes->count() > 0)
                <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg mt-8">
                    <div class="p-6">
                        <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-6">
                            🏆 Classement des Mèmes
                        </h3>

                        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                            @foreach($rankedMemes as $index => $meme)
                                <div class="relative bg-gray-50 dark:bg-gray-700 rounded-lg overflow-hidden hover:shadow-lg transition duration-300">
                                    <!-- Position dans le classement -->
                                    <div class="absolute top-2 left-2 z-10">
                                        @if($index === 0)
                                            <span class="bg-yellow-500 text-white text-lg font-bold px-3 py-1 rounded-full shadow-lg">
                                                🥇 1er
                                            </span>
                                        @elseif($index === 1)
                                            <span class="bg-gray-400 text-white text-lg font-bold px-3 py-1 rounded-full shadow-lg">
                                                🥈 2ème
                                            </span>
                                        @elseif($index === 2)
                                            <span class="bg-orange-600 text-white text-lg font-bold px-3 py-1 rounded-full shadow-lg">
                                                🥉 3ème
                                            </span>
                                        @else
                                            <span class="bg-gray-600 text-white text-sm font-bold px-2 py-1 rounded-full shadow-lg">
                                                #{{ $index + 1 }}
                                            </span>
                                        @endif
                                    </div>

                                    <!-- Image du mème -->
                                    <div class="aspect-square overflow-hidden">
                                        <img src="{{ Storage::url($meme->image_path) }}"
                                             alt="Mème {{ $index + 1 }}"
                                             class="w-full h-full object-cover hover:scale-105 transition duration-300 cursor-pointer"
                                             onclick="window.open('{{ route('memes.show', $meme) }}', '_blank')">
                                    </div>

                                    <!-- Informations du mème -->
                                    <div class="p-4">
                                        <div class="flex items-center justify-between mb-2">
                                            <span class="text-sm text-gray-600 dark:text-gray-300">
                                                👤 {{ $meme->user->name }}
                                            </span>
                                            <span class="text-sm font-bold text-blue-600 dark:text-blue-400">
                                                ❤️ {{ $meme->votes_count }} vote(s)
                                            </span>
                                        </div>

                                        <!-- Actions -->
                                        <div class="flex gap-2">
                                            <a href="{{ route('memes.show', $meme) }}"
                                               class="flex-1 bg-blue-600 hover:bg-blue-700 text-white text-center font-bold py-2 px-3 rounded-lg transition duration-200 text-sm">
                                                👁️ Voir
                                            </a>

                                            @auth
                                                @if($battle->isOpen() && !$meme->hasUserVoted(Auth::user()) && $meme->user_id !== Auth::id())
                                                    <form method="POST" action="{{ route('votes.store', $meme) }}" class="flex-1">
                                                        @csrf
                                                        <button type="submit"
                                                                class="w-full bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-3 rounded-lg transition duration-200 text-sm">
                                                            ❤️ Voter
                                                        </button>
                                                    </form>
                                                @elseif($meme->hasUserVoted(Auth::user()))
                                                    <form method="POST" action="{{ route('votes.destroy', $meme) }}" class="flex-1">
                                                        @csrf
                                                        @method('DELETE')
                                                        <button type="submit"
                                                                class="w-full bg-gray-600 hover:bg-gray-700 text-white font-bold py-2 px-3 rounded-lg transition duration-200 text-sm">
                                                            💔 Retirer
                                                        </button>
                                                    </form>
                                                @endif

                                                @can('delete', $meme)
                                                    <form method="POST" action="{{ route('memes.destroy', $meme) }}"
                                                          onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce mème ?')">
                                                        @csrf
                                                        @method('DELETE')
                                                        <button type="submit"
                                                                class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-3 rounded-lg transition duration-200 text-sm">
                                                            🗑️
                                                        </button>
                                                    </form>
                                                @endcan
                                            @endauth
                                        </div>
                                    </div>
                                </div>
                            @endforeach
                        </div>
                    </div>
                </div>
            @else
                <!-- Message si aucun mème -->
                <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg mt-8">
                    <div class="p-12 text-center">
                        <div class="text-6xl mb-4">🎭</div>
                        <h3 class="text-xl font-semibold text-gray-900 dark:text-white mb-2">
                            Aucun mème soumis
                        </h3>
                        <p class="text-gray-600 dark:text-gray-300 mb-6">
                            @if($battle->isOpen())
                                Soyez le premier à soumettre un mème pour cette battle !
                            @else
                                Cette battle est fermée et aucun mème n'a été soumis.
                            @endif
                        </p>
                        @auth
                            @if($battle->isOpen())
                                <a href="{{ route('memes.create', $battle) }}"
                                   class="bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200 shadow-lg">
                                    🎭 Soumettre le premier mème
                                </a>
                            @endif
                        @endauth
                    </div>
                </div>
            @endif
        </div>
    </div>
</x-app-layout>
