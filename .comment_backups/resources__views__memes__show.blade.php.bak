<x-app-layout>
    <x-slot name="header">
        <div class="flex items-center">
            <a href="{{ route('battles.show', $meme->battle) }}" 
               class="mr-4 text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white">
                ← Retour à la battle
            </a>
            <h2 class="font-semibold text-xl text-gray-800 dark:text-gray-200 leading-tight">
                🎭 Mème de {{ $meme->user->name }}
            </h2>
        </div>
    </x-slot>

    <div class="py-6">
        <div class="max-w-4xl mx-auto sm:px-6 lg:px-8">

            <!-- Informations de la battle -->
            <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg mb-6">
                <div class="p-6">
                    <div class="flex items-center justify-between mb-4">
                        <div>
                            <h3 class="text-lg font-bold text-gray-900 dark:text-white">
                                🏆 {{ $meme->battle->title }}
                            </h3>
                            <p class="text-gray-600 dark:text-gray-300 text-sm">
                                {{ Str::limit($meme->battle->description, 100) }}
                            </p>
                        </div>
                        <div class="text-right">
                            <div class="text-sm text-gray-500 dark:text-gray-400">
                                <div>📅 Deadline: {{ $meme->battle->deadline->format('d/m/Y à H:i') }}</div>
                                <div class="mt-1">
                                    <span class="px-2 py-1 text-xs font-semibold rounded-full
                                        {{ $meme->battle->isOpen() ? 'bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-200' : 'bg-red-100 text-red-800 dark:bg-red-900 dark:text-red-200' }}">
                                        {{ $meme->battle->isOpen() ? '🟢 Ouverte' : '🔴 Fermée' }}
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                    <a href="{{ route('battles.show', $meme->battle) }}" 
                       class="text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 text-sm font-medium">
                        ← Voir tous les mèmes de cette battle
                    </a>
                </div>
            </div>

            <!-- Mème principal -->
            <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg mb-6">
                <div class="p-6">
                    <div class="text-center">
                        <!-- Image du mème -->
                        <div class="mb-6">
                            <img src="{{ Storage::url($meme->image_path) }}" 
                                 alt="Mème de {{ $meme->user->name }}"
                                 class="max-w-full h-auto mx-auto rounded-lg shadow-lg hover:shadow-xl transition duration-300 cursor-pointer"
                                 onclick="window.open(this.src, '_blank')">
                        </div>

                        <!-- Informations du mème -->
                        <div class="flex items-center justify-between mb-6">
                            <div class="flex items-center space-x-4">
                                <div class="text-left">
                                    <div class="text-sm text-gray-500 dark:text-gray-400">
                                        👤 Soumis par <strong>{{ $meme->user->name }}</strong>
                                    </div>
                                    <div class="text-sm text-gray-500 dark:text-gray-400">
                                        📅 {{ $meme->created_at->format('d/m/Y à H:i') }}
                                    </div>
                                </div>
                            </div>
                            
                            <div class="text-right">
                                <div class="text-2xl font-bold text-red-600 dark:text-red-400 mb-1">
                                    ❤️ {{ $meme->votes_count }} vote(s)
                                </div>
                                <div class="text-sm text-gray-500 dark:text-gray-400">
                                    Position dans le classement
                                </div>
                            </div>
                        </div>

                        <!-- Actions de vote -->
                        @auth
                            @if($meme->battle->isOpen())
                                @if($meme->user_id !== Auth::id())
                                    @if($meme->hasUserVoted(Auth::user()))
                                        <div class="bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800 rounded-lg p-4 mb-6">
                                            <p class="text-green-800 dark:text-green-200 text-center">
                                                ✅ Vous avez voté pour ce mème !
                                            </p>
                                            <form method="POST" action="{{ route('votes.destroy', $meme) }}" class="mt-3">
                                                @csrf
                                                @method('DELETE')
                                                <button type="submit" 
                                                        class="bg-gray-600 hover:bg-gray-700 text-white font-bold py-2 px-6 rounded-lg transition duration-200">
                                                    💔 Retirer mon vote
                                                </button>
                                            </form>
                                        </div>
                                    @else
                                        <div class="bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg p-4 mb-6">
                                            <p class="text-blue-800 dark:text-blue-200 text-center mb-3">
                                                Ce mème vous fait rire ? Donnez-lui votre vote !
                                            </p>
                                            <form method="POST" action="{{ route('votes.store', $meme) }}">
                                                @csrf
                                                <button type="submit" 
                                                        class="bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-8 rounded-lg transition duration-200 shadow-lg text-lg">
                                                    ❤️ VOTER POUR CE MÈME
                                                </button>
                                            </form>
                                        </div>
                                    @endif
                                @else
                                    <div class="bg-yellow-50 dark:bg-yellow-900/20 border border-yellow-200 dark:border-yellow-800 rounded-lg p-4 mb-6">
                                        <p class="text-yellow-800 dark:text-yellow-200 text-center">
                                            👤 Vous ne pouvez pas voter pour votre propre mème
                                        </p>
                                    </div>
                                @endif
                            @else
                                <div class="bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded-lg p-4 mb-6">
                                    <p class="text-gray-600 dark:text-gray-300 text-center">
                                        ⏰ Cette battle est fermée. Les votes ne sont plus acceptés.
                                    </p>
                                </div>
                            @endif
                        @else
                            <div class="bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg p-4 mb-6">
                                <p class="text-blue-800 dark:text-blue-200 text-center">
                                    🔐 <a href="{{ route('login') }}" class="underline hover:no-underline">Connectez-vous</a> 
                                    pour voter pour ce mème !
                                </p>
                            </div>
                        @endauth

                        <!-- Actions de gestion -->
                        @auth
                            @can('delete', $meme)
                                <div class="border-t border-gray-200 dark:border-gray-600 pt-4">
                                    <form method="POST" action="{{ route('memes.destroy', $meme) }}" 
                                          onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce mème ? Cette action est irréversible.')">
                                        @csrf
                                        @method('DELETE')
                                        <button type="submit" 
                                                class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-lg transition duration-200">
                                            🗑️ Supprimer ce mème
                                        </button>
                                    </form>
                                </div>
                            @endcan
                        @endauth
                    </div>
                </div>
            </div>

            <!-- Statistiques détaillées -->
            <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg">
                <div class="p-6">
                    <h3 class="text-lg font-bold text-gray-900 dark:text-white mb-4">
                        📊 Statistiques
                    </h3>
                    
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                        <div class="text-center p-4 bg-gray-50 dark:bg-gray-700 rounded-lg">
                            <div class="text-2xl font-bold text-blue-600 dark:text-blue-400">
                                {{ $meme->votes_count }}
                            </div>
                            <div class="text-sm text-gray-600 dark:text-gray-300">
                                ❤️ Votes reçus
                            </div>
                        </div>
                        
                        <div class="text-center p-4 bg-gray-50 dark:bg-gray-700 rounded-lg">
                            <div class="text-2xl font-bold text-green-600 dark:text-green-400">
                                {{ $meme->battle->memes->count() }}
                            </div>
                            <div class="text-sm text-gray-600 dark:text-gray-300">
                                🎭 Mèmes dans cette battle
                            </div>
                        </div>
                        
                        <div class="text-center p-4 bg-gray-50 dark:bg-gray-700 rounded-lg">
                            <div class="text-2xl font-bold text-purple-600 dark:text-purple-400">
                                {{ $meme->battle->memes->sum('votes_count') }}
                            </div>
                            <div class="text-sm text-gray-600 dark:text-gray-300">
                                🗳️ Votes totaux
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</x-app-layout>
